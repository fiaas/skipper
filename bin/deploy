#!/usr/bin/env bash

set -evuo pipefail

echo "${DOCKER_PASSWORD}" | docker login -u "${DOCKER_USERNAME}" --password-stdin

export VERSION=$(python setup.py --version)
export TAG=$(bin/tag_release|tail -1)

echo "using version $VERSION"
echo "using tag $TAG"

bin/docker_push
bin/publish_helm_chart
